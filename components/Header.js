import React from 'react';
import styles from './header.module.scss';

const Header = () => {
  return (
    <div className={styles.class}>
      <h1>Hello i'm the header component using the sass for styles</h1>
    </div>
  );
};

export default Header;
